// FavQs API Configuration
// This file is loaded before the main app to ensure configuration is ready

// Note: The API key is now loaded automatically from the .env file
// If you want to override it manually, uncomment and modify the following:

// import { setFavQsApiKey } from './config.js';
// setFavQsApiKey('your_api_key_here');

// The application will work without an API key using local quotes as fallback
